// Auto-generated by schema engine. Do not edit manually.
// Edit schema.yaml instead and restart the app.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Patient {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  name String
  date_of_birth DateTime?
  medicare_number String? @unique
  phone String?
  email String?
  address String?
  status String?
  maintenance_plan_expiry DateTime?
  notes String?
  referrals Referral[]
  clinical_notes ClinicalNote[]
  personal_notes PersonalNote[]
  hearing_aids HearingAid[]
  claim_items ClaimItem[]
  appointments Appointment[]
  attachments Attachment[]
}

model Referral {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  referring_gp String
  gp_practice String?
  referral_date DateTime
  reason String?
  expiry_date DateTime?
  notes String?
  patientId Int?
  patient Patient? @relation(fields: [patientId], references: [id])
  @@index([patientId])
}

model ClinicalNote {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  date DateTime
  note_type String?
  content String
  clinician String?
  patientId Int?
  patient Patient? @relation(fields: [patientId], references: [id])
  attachments Attachment[]
  @@index([patientId])
}

model PersonalNote {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  date DateTime
  content String
  patientId Int?
  patient Patient? @relation(fields: [patientId], references: [id])
  @@index([patientId])
}

model HearingAid {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ear String?
  make String?
  model String?
  serial_number String?
  battery_type String?
  wax_filter String?
  dome String?
  programming_cable String?
  programming_software String?
  hsp_code String?
  warranty_end_date DateTime?
  last_repair_details String?
  repair_address String?
  patientId Int?
  patient Patient? @relation(fields: [patientId], references: [id])
  @@index([patientId])
}

model ClaimItem {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  item_number String
  description String?
  date_of_service DateTime
  amount Float?
  status String?
  notes String?
  patientId Int?
  patient Patient? @relation(fields: [patientId], references: [id])
  @@index([patientId])
}

model Appointment {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  date DateTime
  start_time String
  end_time String
  location String
  specialty String
  status String?
  notes String?
  patientId Int?
  patient Patient? @relation(fields: [patientId], references: [id])
  nurseId Int?
  nurse Nurse? @relation(fields: [nurseId], references: [id])
  @@index([date])
  @@index([patientId])
  @@index([nurseId, date])
}

model Nurse {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  name String
  phone String?
  email String? @unique
  registration_number String?
  caldav_url String?
  google_calendar_id String?
  notes String?
  appointments Appointment[]
  nurse_specialtys NurseSpecialty[]
  calendar_connections CalendarConnection[]
}

model NurseSpecialty {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  specialty String
  notes String?
  nurseId Int?
  nurse Nurse? @relation(fields: [nurseId], references: [id])
  @@index([nurseId])
}

model CalendarConnection {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  provider String?
  calendar_id String?
  access_token String?
  refresh_token String?
  token_expiry DateTime?
  nurseId Int?
  nurse Nurse? @relation(fields: [nurseId], references: [id])
  @@index([nurseId])
}

model Attachment {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  filename String
  storage_path String
  mime_type String?
  size_bytes Float?
  category String?
  description String?
  patientId Int?
  patient Patient? @relation(fields: [patientId], references: [id])
  clinical_noteId Int?
  clinical_note ClinicalNote? @relation(fields: [clinical_noteId], references: [id])
  @@index([patientId])
  @@index([clinical_noteId])
}

model User {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  email String @unique
  name String
  password_hash String
  role String
  active Boolean? @default(true)
  sessions Session[]
  audit_logs AuditLog[]
}

model Session {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  token String @unique
  last_active DateTime
  expires_at DateTime
  ip String?
  user_agent String?
  userId Int?
  user User? @relation(fields: [userId], references: [id])
  @@index([userId])
}

model AuditLog {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  timestamp DateTime
  action String
  entity String
  entity_id String
  details String?
  ip String?
  user_agent String?
  userId Int?
  user User? @relation(fields: [userId], references: [id])
  @@index([timestamp])
  @@index([userId])
  @@index([entity, entity_id])
}

model ClaimToken {
  id        Int       @id @default(autoincrement())
  email     String
  tokenHash String    @unique
  usedAt    DateTime?
  expiresAt DateTime
  createdAt DateTime  @default(now())
}
